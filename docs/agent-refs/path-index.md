# Path index — where everything lives

Written for agents. Look here **before** grepping the repo.

Legend: 🟢 Studio (active work) · 🟡 shared infrastructure Studio depends on ·
⚪ dormant CMS — do not build on it, do not delete it.

---

## Studio: server side

| Path | What it owns |
|---|---|
| 🟢 `server/handlers/studio.ts` | **HTTP routing only** for every `/admin/api/studio/*` endpoint. Its module doc lists all routes — read it first. |
| 🟢 `server/handlers/studioPageLoad.ts` | The parse → inline → CSS → convert pipeline. `resolveModuleId` (element → module) lives here. WS-10 §4.2/§4.4 (Phase 4) — also exports `loadStudioPageInLocale(dir, pageId, locale)`, parsing ONE route under an explicit `preferredKey` override (reuses `parseStandardRouteEntry`/`parseAppRouterRouteEntry`, extracted from the whole-site builders for this). |
| 🟢 `server/handlers/studio/localizedPage.ts` | WS-10 §4.4 (Phase 4) — `GET /admin/api/studio/localized-page`, the `(pageId, locale) → Page \| null` route `localizedPageSlice.ts` fetches on demand. |
| 🟢 `server/handlers/studioProjects.ts` | Project discovery, `.studio/meta.json`, `discoverPageFiles`, `projectPagesDir`, `nextPageName`. |
| 🟢 `server/handlers/studio/pageTemplates.ts` | `starterPage(componentName, kind)` — the starter `.tsx` + `.module.css` written for each `PageKind` (screen / popup / small + big bottom sheet). Dependency-free and canonical by construction; gated by `__tests__/pageTemplates.test.ts`. |
| 🟢 `server/handlers/studioWriteback.ts` | `StudioEdit` shapes, `studioEditLocation`, dedupe, path containment, `applyStudioEdit`. |
| 🟠 `server/handlers/studioStructuralWriteback.ts` | The `move`/`delete`/`insert` edit kinds — schemas + dispatch into the structural codemods. Sibling of `studioCssWriteback.ts`; `studioWriteback.ts` imports it, never the reverse. |
| 🟢 `server/handlers/studioCss.ts` | Imported `.css` → `StyleRule` registry, deterministic ids, happy-dom CSSOM. `loadStudioStyles`'s `extraCss` param merges in `styleCompile.ts`'s compiled output. `board-27` — ALSO returns `authoredCss`, the same stylesheets' RAW text (byte-faithful; the `StyleRule` registry loses declarations happy-dom's CSSOM can't parse). |
| 🟢 `server/handlers/studio/projectProbe.ts` | WS-1.2 — `dir → ProjectProfile`: framework, pages dir, aliases, component packages, `detectColorScheme` (WS-10 §3.1 — Tailwind `darkMode: 'class'`, a `.dark`/`[data-theme="dark"]` selector, or `@media (prefers-color-scheme: dark)`, purely syntactic; scans the project's own stylesheets FIRST, then its installed component packages' — a project themed entirely by its design system used to report `'none'`), `detectLocales` (WS-10 §4.1, `./localeProbe.ts` — a `translations[lang]`-style dictionary index, an i18next/react-intl `resources` map, or a `locales/*.json` directory; also purely syntactic, `undefined` when none is found), and (`approot-01`) app-root detection (`detectAppRoot`) — a project's app root is not always its project directory (monorepos, `journey-screens/`-style named subdirectories). Every OTHER detector runs rooted at the resolved app root; every returned path is re-prefixed with `appRoot` to stay project-relative. `pagesDir` ranking (`rankPagesDirCandidates`) scores a candidate's whole RECURSIVE subtree, not just its direct files. Style-toolchain detection itself lives in `styleToolchainDetect.ts` (split out to stay under the module-size budget after `designSystems` landed). Also exports `tryServeStudioProbe`, wired into `STUDIO_SUB_ROUTERS`, and `resolveProjectProfilePersisting` (perf-06) — same cache/stale/fresh logic as `resolveProjectProfile`, except the "no cache at all" branch persists the freshly-probed profile instead of returning it un-persisted; used by callers (`projectGuide.ts`) that already write other files to disk once per turn, so a project with no `package.json`/`node_modules` (and so no install-triggered cache heal) stops re-probing from scratch on every call. |
| 🟢 `server/handlers/studio/packageJsonRead.ts` | Tiny shared leaf: `PackageJsonSchema`/`PackageJsonShape`, `readPackageJson`, `hasDependency`, `findConfigFile` — used by both `projectProbe.ts` (framework shape, app-root scoring) and `styleToolchainDetect.ts` without either importing the other. |
| 🟢 `server/handlers/studio/styleToolchainDetect.ts` | `detectStyleToolchain(root, pkg, warnings) → ProjectProfile['styleToolchain']` — Tailwind v3 (config file)/v4 (`@import "tailwindcss"` in a stylesheet), Sass, CSS Modules, CSS-in-JS, and `postcssConfigPath`. Extracted out of `projectProbe.ts` to keep that module under `module-size-budgets.test.ts`'s 700-line ceiling — one detection question, one file, same pattern as `componentPackageDetect.ts`/`colorSchemeDetect.ts`/`designSystemDetect.ts`. |
| 🟢 `server/handlers/studio/colorSchemeDetect.ts` | `detectColorScheme(appRoot, componentPackages)` — the dark-mode `ColorSchemeCapability`. Two ordered passes: the project's own stylesheets (`listWorkspaceFiles`, which excludes `node_modules`), then the stylesheets its installed component packages ship (`dist/`, `build/`, `lib/`, the package root — never a recursive walk). The project wins; a package's gate is reported only when the project declares none. `source` names the winning file. |
| 🟢 `server/handlers/studio/localeProbe.ts` | WS-10 §4.1 — `detectLocales(root)`: two-pass scan (every top-level object-literal declaration in the project, by name; then every `<name>[<indexExpr>]` dynamic-index access) so a dictionary declared in one file and indexed in another (confirmed against the real eSIM corpus: `translations.js` / `LanguageContext.jsx`) is still found. `extractTopLevelKeys` is a brace/string-aware depth-1 key scanner shared by the dictionary-index rule and the i18next/react-intl `resources` rule. |
| 🟢 `server/handlers/studio/projectProfileSchema.ts` | Pure schema leaf: `ProjectProfileSchema`/`ProjectProfile`, `ProbeWarning`, `PROBE_VERSION` (bumped when a detector starts finding something it previously could not; `isProfileStale` re-probes a cache below it), `ColorSchemeCapability` (WS-10 §3.1 — `mechanism: 'media'\|'class'\|'none'` + detected `selector` + the `source` file it was found in), `LocalesCapability` (WS-10 §4.1 — `keys`/`defaultKey?`/`source`), `DesignSystemRef` (`name`/`source: 'node-modules'\|'imported'`/`root`) — kept separate from `projectProbe.ts` to avoid a load cycle with `studioMeta.ts` (which persists a probe result and is read back by the probe for caching). |
| 🟢 `server/handlers/studio/designSystemDetect.ts` | `detectDesignSystems` — `ProjectProfile.designSystems`: one entry per `componentPackages` name (`node_modules`) AND one per immediate `styles/imported/<slug>/` subdirectory (the design-token import wizard's CSS-only copy, `designImport.ts` — no `package.json` involved at all). Decouples design-system detection from `node_modules` so a project with no manifest still gets one. |
| 🟢 `server/handlers/studio/designSystemDigest.ts` | `getOrBuildDesignSystemDigest` — the generated `.claude/design-system.md` reference (`projectGuide.ts`): all five token families (colors/typography/spacing/**radius**/**elevation**, the last two `FrameworkSettings` has no home for) plus a one-line-per-component BEM index (class + variants + source file), built from `tokenExtractCssScan.ts`'s shared engine. Cached `.studio/cache/design-system-<hash>.md`, same convention as `styleCompile.ts`. Also exports `computeDesignSystemCacheKey` (stat-based CSS fingerprint) for reuse by `projectGuideManifest.ts`'s regeneration gate. |
| 🟢 `server/handlers/studio/projectGuide.ts` | `generateStudioProjectGuide(dir)` — writes the project's own `CLAUDE.md` (root; the CLI loads it from cwd for free) plus `.claude/design-system-components.md` and `.claude/design-system.md`, called from `claudeCli.ts` before every real chat turn. Replaced the eleven-agent subagent roster, which existed only because the agent had no filesystem. Regeneration fingerprint/manifest mechanics live in `projectGuideManifest.ts` — this file owns the CONTENT and the never-clobber write loop, that one owns "should we even bother rebuilding". |
| 🟢 `server/handlers/studio/projectMcpApprovals.ts` | `resolveApprovedMcpServerNames` — every MCP server name this project has actually approved, from BOTH lists (`.mcp.json` names in `.studio/meta.json`'s `approvedMcpServers`, and Studio-registered servers in `approvedRegisteredMcpServers`), read via `../../ai/drivers/{projectMcpServers,registeredMcpServers}.ts` and never a broader guess. Also `mcpServerFingerprintWitness`, folded into `computeProjectGuideFingerprint` so approving/revoking a server forces a full regeneration. |
| 🟢 `server/handlers/studio/projectGuideManifest.ts` | `computeProjectGuideFingerprint`/`allOwnedFilesUnchangedSince`/`readManifest`/`writeManifest`: gates `projectGuide.ts`'s regeneration behind a cheap input fingerprint (profile + design-system CSS stat key + package-docs stat witness + `GUIDE_DEFINITION_VERSION` + `mcpServerFingerprintWitness`) AND a per-target-file stat check (the clobber-protection half — a hand-edited output file has an unchanged fingerprint but a stat mismatch). **Bump `GUIDE_DEFINITION_VERSION` whenever a generated file is added or removed.** |
| 🟢 `server/handlers/studio/designSystemGuide.ts` | `buildDesignSystemGuide`/`renderComponentReference` — reads an installed design system's own `design.md`/`CLAUDE.md` and keeps the two parts that answer "which component" (the Component Decision Map, inlined into the generated `CLAUDE.md`) and "what props" (each `### <Name>` section's props block, written to `.claude/design-system-components.md`). The import contract is BUILT from the installed package name + its `exports` map, never copied from the docs — ALM's own docs say `from 'design-system'`, which would resolve to nothing. |
| 🟢 `server/handlers/studio/appRoot.ts` | `approot-01` — `resolveAppRoot(dir)`/`joinAppRoot(dir, appRoot)`: `dir → absolute, real-path containment-checked app root`. The one place every `node_modules`/toolchain-touching consumer (`installDeps.ts`, `styleCompile.ts`, `componentBundle.ts`, `packageManifest.ts`'s callers) resolves the app root, instead of five separate joins that can drift apart. |
| 🟢 `server/handlers/studio/studioMeta.ts` | Schema-validated `.studio/meta.json` (`StudioMetaSchema`): `displayName`, `pagesDir` override, legacy `previewLocale` (folded into `previewAxes.locale` on read by `foldLegacyPreviewLocale`, never returned itself — WS-10 §5.2), `previewAxes` (`direction`/`colorScheme`/`locale`, all optional — WS-10 Phase 1 + §4.2), `trust`, cached `profile` (`ProjectProfileSchema`, includes `appRoot`), `frameDefaults`, `paletteHiddenModuleIds`. |
| 🟢 `server/handlers/studio/styleCompile.ts` | WS-2.1/2.3 — `dir + ProjectProfile → CompiledStyles`. CSS Modules (Tier 0, own transform) + vendor package CSS (`vendorCss` — bare-specifier `.css` imports resolved against the project's own `node_modules`, Tier 0 safe, no trust gate) + `compileProjectStyles`'s overall orchestration + the on-disk cache, cached under `.studio/cache/styles-<hash>.*`. Tier 1 (Sass/PostCSS/Tailwind) itself lives in `styleCompileTier1.ts` (split out to stay under the module-size budget). |
| 🟢 `server/handlers/studio/styleCompileTier1.ts` | `compileSass`/`compilePostcssPipeline` — resolves the workspace's own `sass`/`postcss`/`@tailwindcss/postcss`, gated on trust, spawns `styleCompileWorker.ts` to actually run them. |
| 🟢 `server/handlers/studio/styleCompileWorker.ts` | `sec-01` — the Tier 1 SUBPROCESS entry point. Runs `sass`/`postcss`/`postcss.config.js`/plugin packages OUT of the admin server's own process; reads one `WorkerTask` from argv, writes one `WorkerResult` to stdout. |
| 🟢 `server/handlers/studio/styleCompileFileRead.ts` | Tiny shared leaf: `readCappedFile`, `CSS_MODULE_FILE_RE`, `MAX_STYLESHEET_BYTES` — used by both `styleCompile.ts` and `styleCompileTier1.ts` without a cycle. |
| 🟢 `server/handlers/studio/subprocessRunner.ts` | `sec-01` — the shared spawn+timeout+capped-capture primitive (`runCappedSubprocess`/`captureSubprocess`) and `minimalSubprocessEnv`, used by `styleCompileTier1.ts` and `installDeps.ts`. |
| 🟢 `server/handlers/studio/workspacePackageResolve.ts` | `sec-01` — `resolveWorkspacePackageEntry`/`isRealpathContained`: symlink-containment-checked `<dir>/node_modules/<pkg>` resolution, used by the parent (`styleCompileTier1.ts`) and the subprocess (`styleCompileWorker.ts`) alike. |
| 🟢 `server/handlers/studio/installDeps.ts` | WS-1.4 — `bun install`/etc as a polled job. `sec-01`: uses `subprocessRunner.ts`'s shared capture primitive, explicit minimal env. `approot-01`: `cwd` is the resolved app root (`appRoot.ts`), not necessarily the project directory. `infra-01`: mirrors every job to `.studio/install-job.json` (`installJobStore.ts`) so a `bun --watch` restart resolves to a terminal `'interrupted'` status instead of a phantom `'running'` the client polls forever — see `resolveInstallJobStatus`/`resolvePersistedJobStatus`. |
| 🟢 `server/handlers/studio/installJobStore.ts` | `infra-01` — read/write for `<appRoot>/.studio/install-job.json`, the durability sidecar `installDeps.ts` mirrors its job registry to. Defensive read (never throws), unconditional overwrite (single most-recent job per project, no merge/history). |
| 🟢 `server/handlers/studio/packageManifestSchema.ts` | `pkg-01`/WS-3.1 — pure schema leaf: `PropKind` (the string/number/boolean/enum/color/image/node/handler/unknown classification), `PropSpec`, `ComponentSpec`. |
| 🟢 `server/handlers/studio/packageManifest.ts` | `pkg-01`/WS-3.1 — `dir + package name → ComponentSpec[]`, purely syntactic (`.d.ts` then `.tsx` source, never the type checker, never executes anything — Tier 0 safe on its own). Replaces `scripts/gen-alm-manifest.mjs`'s build-time, `@alm-design`-only extraction with a per-project, per-package one. |
| 🟢 `server/handlers/studio/componentBundle.ts` | `pkg-01`/WS-3.2 — `POST/GET /admin/api/studio/component-bundle`: `tryServeStudioComponentBundle` sub-router. Tier 1 gate, React major-version skew check, demand list (`ProjectProfile.componentPackages`), barrel generation, `.studio/cache/bundle-<hash>.{js,json}`. Wired into `STUDIO_SUB_ROUTERS`. `approot-01`: the react-version check, cache key, manifest extraction, and the generated barrel entry (placed directly at the resolved app root so `Bun.build`'s upward `node_modules` walk finds it) all resolve against the app root, not necessarily the project directory; the artefact itself stays at `.studio/cache/` under the project directory. |
| 🟢 `server/handlers/studio/componentBundleWorker.ts` | `pkg-01`/WS-3.2 — the Tier 1 SUBPROCESS entry point: runs `Bun.build` (a package can execute a Bun macro at build time) out of the admin server's own process, via `subprocessRunner.ts`/`minimalSubprocessEnv()`, same posture as `styleCompileWorker.ts`. |
| 🟢 `server/handlers/studio/trustTier.ts` | `pkg-02`/WS-3.3 — `GET/POST /admin/api/studio/trust-tier`: `tryServeStudioTrustTier` sub-router. Reads/writes `.studio/meta.json`'s `trust` field — the action behind the canvas's "promote this project" placeholder. |
| 🟢 `server/handlers/studio/previewAxes.ts` | WS-10 Phase 1 — `GET/POST /admin/api/studio/preview-axes`: `tryServeStudioPreviewAxes` sub-router. Reads/writes `.studio/meta.json`'s `previewAxes` field via a merge-patch POST (same shape as `trustTier.ts`). |
| 🟢 `server/handlers/studio/translationCatalog.ts` | Reads the project's OWN locale dictionary into a flat `key -> { en, ar }` table. Handles all three shapes `localeProbe` detects (a `translations[lang]` module, an i18next `resources` config, a `locales/*.json` directory); nested keys flatten with dots. `null` when the project declares no locales — a different fact from an empty table. |
| 🔴 `server/handlers/studio/translationWrite.ts` | Sets one `(locale, key)` in that dictionary, CREATING a missing locale branch/intermediate object/leaf (the normal case). Refuses to overwrite a value that is code rather than a string literal. Shares `findLocaleRootLiteral` with the reader so the writer can never target a literal the reader did not show. |
| 🔴 `server/handlers/studio/hardcodedStrings.ts` | Every copy-shaped string literal still written inline in the JSX, with the location an extraction would rewrite. The whole design is the FILTER: `<svg>` subtrees are excluded wholesale — per LITERAL, not per prop, since `icon={<svg …/>}` puts geometry inside a copy prop (a path's `d` defeats every sentence heuristic — measured 4 of 14 hits on `untitled-2`) — plus a machinery-prop list and a digit-ratio guard. A single word is copy when it is CAPITALISED (`From`, `Dates`) and an enum token when it is not (`primary`, `solid`). Also reads literals nested in a prop expression (`toolbar={{ title: 'Account' }}`, `items={['Flights']}`), which is how this corpus's design system takes structured props. Biased toward missing a string over inventing one. |
| 🔴 `server/handlers/studio/i18nScaffold.ts` | Writes the two-file i18n module a project with NO localisation needs. Every line of the generated code is load-bearing: `localeProbe` rule 1 must detect it, `staticEvalCalls` Tier B must trace it (exactly one `<Provider>`, `useContext` as the hook's first statement, `t: translations[lang]` as the provider value so §7.4 picks the branch by preview locale), and the hook must work with NO provider mounted — hence a real `createContext` default and no `if (!ctx) throw`, so an extraction can never break a running app. |
| 🔴 `server/handlers/studio/i18nSetup.ts` | `POST /admin/api/studio/i18n-setup` — scaffold, mint a key per scanned string, rewrite the JSX per file, then write the English value for keys that actually landed. `mintKeys` is exported and tested: same text in the same file shares one key, the same text in a different file does not, a name collision takes a suffix. Arabic is deliberately left empty for the explicit translate action. |
| 🔴 `server/ai/oneShot.ts` | `runOneShotCompletion` — one system prompt, one user message, text out. The missing small entry beside the chat runner's tool loop. `tools: []` with a bridge that THROWS, so a driver reaching it surfaces as a bug rather than hanging. |
| 🔴 `server/ai/handlers/translateContent.ts` | `POST /admin/api/ai/translate-content` — batch-translates the untranslated keys in ONE model call (consistent terminology across a screen), validates the JSON reply against a schema before any write, then applies each through `writeTranslationEntry` so a key holding code refuses exactly as a hand edit would. `selectPendingEntries` is exported and tested: no explicit keys means fill gaps, NEVER overwrite. |
| 🟢 `server/handlers/studio/translations.ts` | `GET/POST /admin/api/studio/translations` — the route behind the Content panel. A structured refusal is a 200 `{ ok: false, message }`, not a 4xx. |
| 🟢 `src/admin/pages/site/panels/ContentPanel/` | The Content rail tab: every dictionary key as a row, one input per declared locale, RTL-aware. Commits on blur (each commit is a real source write). Three distinct empty states — no dictionary / no Arabic yet / refused write. |
| 🟢 `server/handlers/studio/iconCatalog.ts` | `GET /admin/api/studio/icons` — the SVG icon FILES a project's installed component packages ship (`@alm-design/design-system`'s 568-file `src/icons/`), which no registry lookup can see. Metadata + markup in one response, per-file capped at 4 KB. Markup is returned RAW: `sanitizeSvg` needs a DOM and Bun has none, so the browser sanitises. No trust tier — `readdir` + `readFile`, nothing evaluated. |
| 🟢 `src/core/module-engine/packageModuleId.ts` | `pkg-02`/WS-3.3 — `packageModuleId`/`sanitizePackageName`: the one `pkg.<sanitized-package>.<ComponentName>` naming scheme, shared by `studioPageLoad.ts`'s `resolveModuleId`, `componentBundle.ts`'s barrel generation, and `registerProjectModules.ts`. |
| 🟢 `src/core/utils/studioSlotSentinel.ts` | `pkg-02`/WS-3.4 — `studio-slot:<nodeId>` sentinel: a package-component prop value referencing a materialized slot child node. Dependency-free (no ts-morph) so both the parser (server) and `registerProjectModules.ts` (browser) can import it. |
| 🟢 `server/handlers/studioAsset.ts` | `GET /admin/api/studio/asset` + all path-containment guards. |
| 🟢 `server/handlers/studioGithubImport.ts` | GitHub zipball fetch (URL parsing, zipball URL, download). |
| 🟢 `server/handlers/studio/archiveIngest.ts` | Shared ingest engine (entry decision, budgets, target-dir clear+write) behind both the GitHub and upload import routes. |
| 🟢 `server/handlers/studio/importUpload.ts` | `POST /admin/api/studio/import-upload` — uploaded `.zip` or a picked folder, via `archiveIngest.ts`. |
| 🟢 `server/handlers/studio/assetUpload.ts` | WS-8.3 — `POST /admin/api/studio/asset-upload`: `tryServeStudioAssetUpload` sub-router, now a thin HTTP shell over `assetLanding.ts`'s `landAssetBytes`. Not yet wired into `STUDIO_SUB_ROUTERS` — see `asset-01` in `STATE.md`. |
| 🟢 `server/handlers/studio/assetLanding.ts` | mcp-tooling — `landAssetBytes`: the ONE write pipeline for landing image/SVG bytes into a project (symlink-aware `targetDir` containment, byte-sniffed content type, SVG sanitized via `sanitizeSvgBytes`, collision-safe filename). Extracted from `assetUpload.ts` so `remoteAssetFetch.ts` (`studio_fetch_remote_asset`) shares it rather than re-implementing the same security-sensitive write path — this extraction also closed a real gap: `assetUpload.ts` previously wrote SVG content unsanitized. |
| 🟢 `server/handlers/studio/remoteAssetFetch.ts` | mcp-tooling — `fetchRemoteAsset` (thin `fetchRemoteBytes` + `landAssetBytes` composition) and `fetchRemoteBytes`, the server-side "fetch this URL" step behind `studio_fetch_remote_asset` — the bytes-never-transit-the-model path for a Figma (or any) MCP export URL. Untrusted-URL-safe: `http(s)` scheme only, no redirect ever followed, streamed-byte-count size cap (`readBytesWithLimit`, shared with the GitHub zipball fetch). `fetchRemoteBytes` is also reused by `designReferenceTools.ts`'s `studio_register_design_reference` (`url` input). |
| 🟢 `server/handlers/studio/designReferenceStore.ts` | mcp-tooling — the durable, addressable-by-id design-reference store: `registerDesignReference`/`listDesignReferences`/`getDesignReference`/`getMostRecentDesignReference`/`readDesignReferenceBytes`/`removeDesignReference`, backed by `.studio/references/manifest.json` + `landAssetBytes`'s one deliberate `.studio` exception (`DESIGN_REFERENCE_ASSET_DIR`, `assetLanding.ts`). Two callers: `studio_register_design_reference`/`studio_list_design_references`/etc. (MCP, `server/ai/mcp/tools/studio/designReferenceTools.ts`) and `POST/GET/DELETE /admin/api/studio/reference-upload` (`referenceUpload.ts`, the chat panel's own attachment control). |
| 🟢 `server/handlers/studio/designReferenceSchema.ts` | Pure schema leaf: `DesignReferenceSchema`/`DesignReferenceManifestSchema` — the shared wire contract `src/core/ai/designReferenceImage.ts`'s `DesignReferenceMetaSchema` mirrors field-for-field. |
| 🟢 `server/handlers/studio/referenceUpload.ts` | `tryServeStudioReferenceUpload` sub-router — the chat panel's HTTP surface over `designReferenceStore.ts`, wired into `STUDIO_SUB_ROUTERS`. |
| 🟢 `server/handlers/studio/boardGeometry.ts` | `readBoardsFileOrEmpty`/`authoredFrameWidth` — headless READ helpers over `.studio/boards.json`, extracted from `editTools.ts`'s local `readBoardsFile` so `studio_recommend_export_dpr` (`designReferenceTools.ts`) shares the same exists-then-parse-else-empty read instead of a second copy. |
| 🟢 `server/handlers/studioDownload.ts` | "Download the code" — zips the workspace. Not codegen. |
| 🟢 `server/handlers/studioFramework.ts` | `.studio/framework.json` sidecar (colors/typography/spacing). |
| 🟢 `server/handlers/studio/tokenExtract.ts` | `tokens-01` — `GET/POST /admin/api/studio/tokens`: `tryServeStudioTokens` sub-router. Orchestrates the three token sources (project-css / tailwind-theme / vendor-css), `extractProjectTokens`, and the never-clobber `mergeExtractedFramework`. Wired into `STUDIO_SUB_ROUTERS`. |
| 🟢 `server/handlers/studio/tokenExtractCssScan.ts` | `tokens-01` — the `:root` custom-property scan + `var()` resolution + value-first classification engine (`classifyCssText`). `infra-01`: `classifyDeclaration`/`resolveVarValue`/`collectRootScopeMaps`/`toPx` are exported and now ALSO the classification engine behind `server/handlers/designImport/parseCssTokens.ts` — one engine, two triggers (automatic vs. manual/external import). |
| 🟢 `server/handlers/studio/tokenExtractTailwind.ts` | `tokens-01` — static (non-executing) `theme.extend` colors/spacing/fontSize reader for a Tailwind config file. |
| 🟢 `server/handlers/studio/tokenExtractBuild.ts` | `tokens-01` — `ClassifiedTokens → FrameworkSettings` (color tokens, one spacing/typography scale group per naming prefix). |

## Studio: parsing and codegen (the engine)

| Path | What it owns |
|---|---|
| 🟢 `src/core/page-parser/parsePageFile.ts` | The ts-morph JSX walk → `ParsedPage`. **Never throws.** |
| 🟢 `src/core/page-parser/inlineLocalComponents.ts` | Local-component expansion, composite ids — WS-4.2: the call site becomes an `instanceOf`-carrying "instance" node (`children` = the inlined subtree), never deleted/spliced. |
| 🟢 `src/core/page-parser/componentSubstitution.ts` | Call-site props → the component's own JSX (`applySubstitutions`). |
| 🟢 `src/core/page-parser/staticLoopExpansion.ts` | `.map` over a resolved array → one node per item. |
| 🟢 `src/core/page-parser/staticEval.ts` | Public composer for the value evaluator. |
| 🟢 `src/core/page-parser/staticEvalCore.ts` | Tier A + recursive walker + binding resolution. |
| 🟢 `src/core/page-parser/staticEvalCalls.ts` | Tier B (hook → provider) + Tier C (pure calls). |
| 🟢 `src/core/page-parser/staticEvalOperators.ts` | Tier A operators, `Math.*`, `\|\|`/`&&`/`??`. |
| 🟢 `src/core/page-parser/staticEvalValues.ts` | Pure leaf: operations on an ALREADY-RESOLVED value — `pluck`, `withNote`, `unresolved`, `originOf`. parser-08: a key missing from a **complete** object/array resolves to `{kind:'undefined'}`, which is a real answer, not `unresolved`. |
| 🟢 `src/core/page-parser/defaultLiteralBindings.ts` | parser-07: a binding's own FIRST-PAINT literal — a `useState(<default>)` arg or a destructured param's `= <default>`. Pure AST leaf; returns the `Node`, never a value. |
| 🟢 `src/core/page-parser/branchSelection.ts` | Which branch renders: multi-return, `? :`, `&&`, `\|\|`, `??`. Records the untaken side as `branchAlternatives`. |
| 🟢 `src/core/page-parser/componentSources.ts` | local vs package classification; the workspace-wide ts-morph `Project` (**tsconfig `paths` already resolve here**). |
| 🟢 `src/core/page-parser/assetImports.ts` | `?raw` text imports, image imports → `studio-asset:` sentinel + `ParsedNode.assetOrigin` (WS-8.3, the import specifier's own location), `.module.css` imports → `{ localName: globalName }` (WS-2.2, sourced from `styleCompile.ts`'s `moduleClassMaps`). |
| 🟢 `src/core/page-parser/inlineSvg.ts` | `<svg>` written as JSX → markup for `base.svg`. |
| 🟢 `src/core/page-parser/jsxAttributeReaders.ts` | How each attribute shape is read. |
| 🟢 `src/core/page-parser/nodeResolution.ts` | Resolved value → `resolution` metadata. Never locks the node — see `withResolution`. |
| 🟢 `src/core/ast-codemods/` | **Every source write.** `setJsxProp`, `setJsxText`, `setJsxStyle`, `setStringLiteral`, `setJsxTagName`, `setImportSpecifier` (WS-8.3), `detachComponent`/`extractComponentCopy`/`swapComponentInstance` (WS-4.4/4.5), `resolveComponentCallSite` (shared "what does this JSX tag refer to" resolution the three of them share), plus the structural trio `moveJsxElement`/`deleteJsxElement`/`insertJsxElement` (`struct-01`) — those three LOCATE with the AST and write a byte-exact splice of the original text (`jsxChildRange.ts`). |
| 🔴 `src/core/ast-codemods/extractStringsToDictionary.ts` | Moves hardcoded copy in ONE file into `{t.key}` lookups, adding the import and `const { t } = useLanguage()` once per file. Batched per file on purpose — the import and the hook are a per-file decision, so a per-string codemod would race its own positions. Edits apply in DESCENDING position order so one never invalidates the next. Refuses per string: a literal outside any component (cannot read a hook), a position whose text changed since the scan, and a file that already binds `t`. |
| 🟢 `src/core/studio-sync/parsedPageToSitePage.ts` | `ParsedPage` → editor `Page` (moduleId, text prop, classIds, codeProps) — WS-4.2: an `instanceOf` node's `props` become `{componentName, source, sourceFile, callSiteProps}`, codeProps prefixed `callSiteProps:<name>`. |
| 🟢 `src/modules/base/instance/` | WS-4.2 — `studio.instance` module: renders `<>{children}</>`, zero DOM. |
| 🟢 `src/core/studio-sync/collectPageStylesheets.ts` | Which `.css` a page depends on, in cascade order. |
| 🟢 `src/core/page-tree/sourceNodeId.ts` | **The node-id grammar.** Separators, decode, "is there one place to write this?" |
| 🟢 `src/core/page-tree/sourceWritability.ts` | `isPropWritableToSource` — the ONE predicate every edit surface asks. |
| 🟢 `src/core/page-tree/styleRuleOrigin.ts` | `board-27` — the shared `sc-` imported-id prefix + `IMPORTED_RULE_TIMESTAMP` (`0`) `studioCss.ts`, `styleRuleWriteback.ts`, and `canvasClassCss.ts` all read instead of re-deriving. |

## Studio: client side

| Path | What it owns |
|---|---|
| 🟢 `src/admin/pages/site/studio/fsCodemodAdapter.ts` | Client → save-route adapter. Studio is the only editor mode; `AdminCanvasLayout` wires this in unconditionally. **Mirrors** `INLINE_ID_SEPARATOR` and `ComponentSource` literals on purpose (importing the parser barrel drags ts-morph into the browser bundle). `saveStudioAssetEdit` (WS-8.3) commits one `kind: 'asset'` edit immediately, outside the ordinary diff loop. WS-10 §4.4 (Phase 4) — `loadSite()` resets + re-subscribes `localizedPageWriteback.ts`'s baseline; `saveSite()` folds `collectLocalizedTextEdits`/`commitLocalizedTextBaseline` in alongside the CSS write-back call. |
| 🟢 `src/admin/pages/site/studio/localizedPageWriteback.ts` | WS-10 §4.4 (Phase 4) — the locale-variant text SAVE path: a `(pageId, locale, nodeId)`-keyed baseline (deliberately separate from `fsCodemodAdapter.ts`'s `loadedValues` — a locale-variant node shares its id with the default tree's node, trap #2). `watchLocalizedPagesForBaseline` seeds a key the instant it's first fetched; `collectLocalizedTextEdits`/`commitLocalizedTextBaseline` are `saveSite`'s diff/advance pair, mirroring `styleRuleWriteback.ts`'s own pattern. |
| 🟢 `src/admin/pages/site/studio/uploadStudioAsset.ts` | WS-8.3 — XHR client for `POST /admin/api/studio/asset-upload` (progress events; the one sanctioned `apiRequest` exception, same as `useUploadQueue`). |
| 🟢 `src/admin/pages/site/studio/boardsApi.ts` | `/admin/api/studio/boards` client. |
| 🟢 `src/admin/pages/site/studio/ImportProjectDialog.tsx` | The import dialog — GitHub / Upload / Local folder tabs, one ingest engine. |
| 🟢 `src/admin/pages/site/studio/importGithubProject.ts` | GitHub import request helper. |
| 🟢 `src/admin/pages/site/studio/importUploadProject.ts` | Upload/local-folder import client (XHR, for progress). |
| 🟢 `src/admin/pages/site/studio/downloadStudioCode.ts` | Download-the-code client. |
| 🟢 `src/admin/pages/site/studio/designImport/` | **Manual** design-token import — user pastes a GitHub URL or npm package spec, previews classified candidates, picks which to apply. Server side: `server/handlers/designImport.ts` + `designImport/parseCssTokens.ts`. `infra-01`: CSS classification now calls straight into `tokenExtractCssScan.ts`'s shared engine (value-first, `var()`-resolved) instead of a separately-drifting name-hint-first classifier — JSON/JS extraction (`extractJsonTokens`/`extractJsTokens`, unique to this manual wizard) stays local, classified through the SAME shared `classifyDeclaration`. Distinct TRIGGER from `tokens-01`'s `tokenExtract.ts` (manual/external source vs. automatic against the currently-open project's own CSS) — one engine, two triggers, not two engines. |
| 🟢 `src/core/studio-comments/` | Review threads (`.studio/comments.json`): TypeBox schemas, tolerant serializer, pure transforms, and **`anchorResolve.ts`** — the five anchor confidences (`exact`/`moved`/`drifted`/`detached`/`unanchored`) and `isAgentActionable`, the single predicate gating whether the agent may act on a thread. Read `docs/features/studio-comments.md` before touching the anchor model. |
| 🟢 `src/core/studio-board/` | `Board`, `BoardFrame`, `StickyNote`, `DocBlock`, `parseBoardsFile`, `devicePresets`, `PreviewAxes`/`DEFAULT_PREVIEW_AXES` (WS-10 Phase 1 — `previewAxes.ts`: `direction`/`colorScheme` render-time, `locale` reserved for Phase 2). Also `MIN_FRAME_SIZE` (`frameGrid.ts`), the shared annotation transforms (`resizeAnnotation`/`reorderAnnotations`/`annotationPaintOrder`, `AnnotationRef`), `platformPresets.ts` — the mobile/web answer a new project is created with, and the `frameDefaults` it becomes — and `pageKinds.ts` (`PageKind`/`PageKindSchema`/`PAGE_KINDS`), the four shapes a new page can be scaffolded as, shared by the scaffold route, the MCP tool and the "New page" menu. |

## Canvas

| Path | What it owns |
|---|---|
| 🟡 `src/admin/pages/site/canvas/IframeFrameSurface.tsx` | The iframe primitive: srcDoc boot, portal, event forwarding. Also applies `previewAxes` to the frame document as an ATTRIBUTE EFFECT (WS-10 Phase 1 — no remount, see `previewAxesFrameEffect.ts`). |
| 🟢 `src/admin/pages/site/canvas/previewAxesFrameEffect.ts` | WS-10 Phase 1 — `applyPreviewAxesToFrameDocument`: sets `dir`/`lang`/`data-studio-scheme`/`data-theme`/`color-scheme` on a frame's `<html>`, plus the project's own class/attribute dark-mode gate when the probe detected one (`parseClassSchemeSelector`). `data-theme` (`VENDOR_THEME_ATTR`) is written in BOTH schemes, never removed — a vendor design system whose light tokens sit under `:root:not([data-theme=light])` reads absence as dark. Also owns `FramePreviewAxesContext`/`useFramePreviewAxes` — the frame's effective axes for components that resolve direction in JS rather than from `html[dir]` — and `useResolvedFrameAxes`, the same board+override merge for a caller OUTSIDE the frame portal (`BreakpointFrame`'s wrapper). |
| 🟢 `src/admin/pages/site/canvas/CanvasFrameContexts.tsx` | The three contexts a mounted frame publishes to its portaled tree — host `<iframe>`, its `Document`, its effective `PreviewAxes` — in one component, so `IframeFrameSurface.tsx` (at the module-size ceiling) does not carry them inline. |
| 🟢 `src/admin/pages/site/canvas/darkSchemeCssTransform.ts` | WS-10 Phase 1 §3.2 — re-gates `@media (prefers-color-scheme: dark\|light)` rules on `html[data-studio-scheme='...']` in the INJECTED COPY only. Brace/comment/string-aware scanner (not a single regex — nested at-rules would break one) + an isolated CSSOM validation per candidate span (never the whole file — happy-dom's CSSOM silently drops `@layer` content). **Each rule's SELECTOR is rewritten into two forms** (`:where(gate) S` and `S:where(gate)`); the original whole-block nesting wrapper inserted a descendant combinator, which made `:root { … }` — the way nearly every project declares its dark palette — match nothing at all. |
| 🟡 `src/admin/pages/site/canvas/CanvasRoot.tsx` | Canvas shell, mode switching, centering, agent snapshot mounting. |
| 🟡 `src/admin/pages/site/canvas/CanvasTransformLayer.tsx` | Pan/zoom container; renders all frames. |
| 🟡 `src/admin/pages/site/canvas/BreakpointFrame.tsx` | One viewport frame (CMS-style responsive frames). |
| 🟢 `src/admin/pages/site/canvas/BoardFramesLayer/` | **The Studio board.** Frames, marquee selection (frames AND annotations), grid, `frameVirtualization.ts`. |
| 🟢 `src/admin/pages/site/canvas/rectResize.ts` | Pure eight-handle resize geometry, shared by frames, sticky notes and doc cards. `minSize` is a required argument — a frame's floor and an annotation's are different numbers. (Was `BoardFramesLayer/frameResize.ts`; moved up when notes/docs gained handles — a note is not a frame.) |
| 🟢 `src/admin/pages/site/canvas/useAnnotationInteraction.ts` | The pointer behaviour a sticky note and a doc card share: select, drag-move with snapping, drag-resize. Pointer capture, never RAF (gated by `overlayRafDiscipline.test.ts`). |
| 🟢 `src/admin/pages/site/canvas/BoardCommentsLayer/` | Comment pins, thread popovers, and the armed `C` tool (`CommentPlacementLayer`). Pins counter-scale by `1 / var(--canvas-zoom)` so they stay a constant on-screen size; they render at their STORED coordinate, never the live rect of the element they name. |
| 🟢 `src/admin/pages/site/canvas/BoardNotesLayer/` | Sticky notes. `useAutoFitText.ts` binary-searches the font size so text fills the note — deliberately NOT applied to doc cards, which own their typography. |
| 🟢 `src/admin/pages/site/canvas/BoardDocsLayer/` | Rich-text doc cards. `docRichText.ts` (execCommand-based commands + `applyInlineStyle` for font family/size), `DocToolbar.tsx` (portaled, anchored by a bounded RAF loop; restores the saved Range before every command — do NOT go back to `preventDefault()` on mousedown, it stops the Selects opening). |
| 🟢 `src/admin/pages/site/canvas/useBoardAnnotationKeyboard.ts` | Delete / Cmd+D / Cmd+C / Cmd+V / arrow-nudge for selected notes and doc cards. Document-level and intent-scoped, like `useCanvasSelectionKeyboard`. |
| 🟢 `src/admin/pages/site/canvas/StudioBoardLayers.tsx` | Board layer composition. |
| 🟡 `src/admin/pages/site/canvas/BreakpointSelectionOverlay.tsx` | Selection/hover rings + chrome placement. |
| 🟡 `src/admin/pages/site/canvas/canvasSelectionOverlayPositioning.ts` | Overlay geometry + write-phase no-op cache. |
| 🟡 `src/admin/pages/site/canvas/canvasDomGeometry.ts` | Cross-iframe measurement, `nodeVisualRect`, `panToCenterBreakpointFrame`. |
| 🟢 `src/admin/pages/site/canvas/InPlaceInspector/` | Floating prop editor for a selected component. |
| 🟡 `src/admin/pages/site/canvas/NodeRenderer.tsx` | Node → React element. Inline-edit binding. `frameId` (via `CanvasFrameContext`) is read BEFORE `selectCanvasPageFor` (TDZ) so a locale-variant frame resolves its node data from `localizedPageSlice.ts` instead of `site.pages` (WS-10 §4.4/Phase 4) — same mechanism the `(frameId, nodeId)` selection/hover scoping already used (WS-10 Phase 2). |
| 🟡 `src/admin/pages/site/canvas/EditorChromeInjector.tsx` | Unlayered editor chrome CSS into each iframe. |
| 🟡 `src/admin/pages/site/canvas/ClassStyleInjector.tsx` | Class registry CSS (`@layer user-authored`). `board-27` — `canvasClassCss.ts`'s `styleRuleNeedsCanvasOverlay` now scopes this to editor-authored + session-edited rules only; an unedited imported rule renders from `AuthoredCssInjector` instead. |
| 🟢 `src/admin/pages/site/canvas/AuthoredCssInjector.tsx` | `board-27` — a project's own CSS injected RAW (`@layer user-authored`, always precedes `mc-classes` in DOM order) so the canvas is byte-faithful to declarations happy-dom's CSSOM would otherwise silently drop. |
| 🟡 `src/admin/pages/site/canvas/UserStylesheetInjector.tsx` | User stylesheets. Pipes the collected CSS through `darkSchemeCssTransform.ts` before injection (WS-10 Phase 1). |
| 🟢 `src/admin/pages/site/canvas/CanvasAnimationInjector.tsx` | Freeze CSS animations in design frames. |
| 🟢 `src/admin/pages/site/canvas/ProjectCssInjector.tsx` | Read-only vendor package CSS into iframes (`@layer vendor`) — Alm's bundled CSS + the open project's own bare-specifier package CSS (WS-2.3). Also pipes it through `darkSchemeCssTransform.ts` (WS-10 Phase 1) — a package's own dark-mode media query gets rewritten too. |
| 🟡 `src/admin/pages/site/canvas/canvasCssLayers.ts` | `reset`/`vendor`/`user-authored` cascade-layer names + ordering pre-declaration. **The reset is the LOWEST layer** — above `vendor` its `:where()` rules annihilate every design system. |
| 🟡 `src/admin/pages/site/canvas/useIframeFrameAutoHeight.ts` | Frame height + the definite `body` height `%` chains need. |
| 🟡 `src/admin/pages/site/canvas/AgentSnapshotFrame.tsx` | Offscreen deterministic frame for agent screenshots. |

## Editor store

| Path | What it owns |
|---|---|
| 🟡 `src/admin/pages/site/store/store.ts` | The composed Zustand store. |
| 🟡 `src/admin/pages/site/store/slices/siteSlice.ts` | Site document, `loadSite`, `saveSite`, tree mutations. |
| 🟡 `src/admin/pages/site/store/slices/site/helpers.ts` | `resolveActiveTreeTarget`, `mutateActiveTree` — **the only place that knows which tree is active**. |
| 🟢 `src/admin/pages/site/store/slices/boardSlice.ts` | Boards, frames, positions, sizes. |
| 🟢 `src/admin/pages/site/store/slices/localizedPageSlice.ts` | WS-10 §4.4 (Phase 4) — `localizedPages: Record<'${pageId}::${locale}', Page>`, a PARALLEL map (not a reshape of `site.pages`). `ensureLocalizedPage` fetches on demand from `GET /admin/api/studio/localized-page`; `updateLocalizedNodeText` is the locale-variant text-edit mutation `inlineEditSlice.ts` calls instead of `updateNodeProps`. |
| 🟡 `src/admin/pages/site/store/slices/selectionSlice.ts` | Node selection, multi-select. |
| 🟡 `src/admin/pages/site/store/slices/canvasSlice.ts` | `canvasView` (design/live), zoom/pan, `runScripts`, `activeBreakpointId`, `previewAxes`/`setPreviewAxes` (WS-10 Phase 1, board-global). |
| 🟡 `src/admin/pages/site/store/slices/inlineEditSlice.ts` | In-place contentEditable sessions. WS-10 §4.4 (Phase 4) — `ActiveInlineEdit` gained `frameId`/`localeOverride`; a session belonging to a locale-variant board frame mutates `localizedPageSlice.ts`'s tree (undo-exempt), never `site.pages`. |
| 🟡 `src/admin/pages/site/store/slices/styleRuleSlice.ts` | Class registry mutations. |
| 🟡 `src/core/page-tree/mutations.ts` | **All tree mutations**, tree-agnostic. |
| 🟡 `src/core/page-tree/treeOperations.ts` | `applyTreeOperation` — the plugin/agent dispatcher over those mutations, plus the structural source gate. |
| 🔴 `src/core/page-tree/sourceStructure.ts` | **Can this node's PLACE be written back?** `refuseStructuralEdit` — the structural sibling of `sourceWritability.ts`. |
| 🔴 `src/admin/pages/site/store/slices/site/structuralSourceEdits.ts` | The store's structural gate: refuse before mutating, resolve the reorder anchor, issue the commit. |

## Panels / UI

| Path | What it owns |
|---|---|
| 🟢 `src/admin/pages/site/panels/PropertiesPanel/` | The right sidebar. `StyleSurface`, `ClassPicker`, `LayoutSection`, `SizeSection`, `PositionSection`, `TypographySection`, `BackgroundSection`, `EffectsSection`, `InteractionSection`, `BorderControl`, `SpacingBoxControl`, `SectionStylesMenu` (apply a generated utility class from the section that owns it), `cssPropertyIcons.ts` (which properties get a mark instead of a caption), `SourceConstraintNotice`, `SharedComponentNotice`, `ImageSourceSection` (WS-8.3 image picker, dispatched from `renderModuleTabContent`). |
| 🟢 `src/admin/pages/site/property-controls/` | Per-prop control dispatch. `CodeValueControl` = read-only stand-in. |
| 🔴 `src/admin/pages/site/property-controls/componentPropKind.ts` | The ONE `PropKind → PropertyControl` mapping, used by every component surface. Also owns `CANVAS_DRIVEN_PROPS` (`dir` — the board's direction axis owns it, not the panel) and the rule that a `handler` prop gets no control. |
| 🟢 `src/admin/pages/site/property-controls/slotCandidates.ts` | What a `node`-kind prop's picker offers, from three sources: the package's shipped icon FILES, its `*Icon` React exports, the project's own components. |
| 🟢 `src/admin/pages/site/property-controls/SlotPicker.tsx` | The picker UI — search, previews, SVG upload. Every source leaves as one `SlotJsxNode`. |
| 🔴 `src/admin/pages/site/property-controls/svgToJsxNode.ts` | SVG markup → the JSX subtree the slot codemod writes. Sanitises first; inline JSX, never an import (an SVG import's meaning depends on the user's bundler). |
| 🟡 `src/admin/pages/site/sidebars/` | LeftSidebar, RightSidebar, PanelRail. |
| 🟡 `src/admin/pages/site/panels/DomPanel/` | Layer tree. |
| 🟢 `src/admin/pages/site/panels/DependenciesPanel/` | Project dependencies. |
| 🟡 `src/ui/components/` | **Shared primitives — every interactive control must use these.** |
| 🟡 `src/styles/globals.css` | **All design tokens.** No hex anywhere else. |

## Modules

| Path | What it owns |
|---|---|
| 🟡 `src/modules/base/` | `base.container`, `base.text`, `base.button`, `base.image`, `base.link`, `base.svg`, … |
| 🟢 `src/modules/alm/register.tsx` | Design-system components as modules, hardcoded to `@alm-design/design-system`, build-time manifest. **Kept per `standing-07`** — not deleted until `registerProjectModules.ts` is PROVEN to render the eSIM board equivalently. |
| 🟢 `src/modules/alm/manifest.generated.json` | 39 component specs. Produced by `scripts/gen-alm-manifest.mjs`. |
| 🟢 `src/admin/pages/site/studio/registerProjectModules.ts` | `pkg-02`/WS-3.3 + WS-3.4 — the generic consumer of `pkg-01`'s manifest/bundle work. `useRegisterProjectModules()` (mounted from `AdminCanvasEditorBody.tsx`) fetches `POST /admin/api/studio/component-bundle` and registers a `pkg.*` module per component, lazily (Tier ≥ 1 + an unregistered `pkg.*` node on the board), undoable on project switch. `revivePropValue` renders WS-3.4 slot sentinels through `NodeRenderer`. |
| 🟢 `src/admin/pages/site/studio/studioProjectTrust.ts` | `pkg-02`/WS-3.3 — per-project trust-tier external store + `promoteProjectToTier1` (the "promote this project" consent action) + the last `component-bundle` refusal status. Split out of `fsCodemodAdapter.ts` to stay under the module-size ceiling. |
| 🟢 `src/admin/pages/site/studio/studioRawCssStores.ts` | `board-27` — the `vendorCss`/`authoredCss` tiny external stores (same pattern as `studioProjectTrust.ts`), re-exported verbatim from `fsCodemodAdapter.ts`. Split out to stay under the module-size ceiling. |
| 🟢 `src/admin/pages/site/canvas/PackageComponentPlaceholder.tsx` | `pkg-02`/WS-3.3 — `NodeRenderer.tsx`'s fallback for an unregistered `pkg.*` node: Tier-0 "promote this project" button, a bundle-refusal message, or a loading state. Styled via `EditorChromeInjector.tsx`'s `[data-studio-package-placeholder]` (renders inside the per-frame iframe — CSS Modules don't reach there). |
| 🟡 `src/core/module-engine/` | Module registry, schema, validation. |
| 🟢 `src/admin/pages/site/studio/previewAxesCapability.ts` | WS-10 — client for `GET/POST /admin/api/studio/preview-axes` (persisted `direction`/`colorScheme`/`locale`) + external stores for BOTH the dark-mode `ColorSchemeCapability` (`mechanism`/`selector`/`source`) and locale `LocalesCapability` probes (`GET /admin/api/studio/probe`, one fetch — `refreshPreviewCapabilities` — refreshes both), same "tiny external store" pattern as `studioProjectTrust.ts`. |
| 🟢 `src/admin/pages/site/studio/usePreviewAxesHydration.ts` | WS-10 Phase 1 — on project open, loads persisted axes into `canvasSlice.previewAxes` + refreshes the color-scheme capability probe. Mounted from `AdminCanvasEditorBody.tsx` alongside `useRegisterProjectModules`. |
| 🟢 `src/admin/pages/site/toolbar/PreviewAxesControls.tsx` | WS-10 §5.1 — the toolbar's direction/dark-mode/locale controls. Dark-mode and locale both render DISABLED WITH THE REASON when the probe found no mechanism/dictionary (§7.4 "probe honesty") — never a silent no-op. Locale is parse-time (§4.2): choosing one persists it then calls `requestCmsSiteReload()`, disabling the `Select` for the duration (`isReparsing`) — direction/colorScheme need neither, they are render-time frame attribute effects. |

## AI / MCP

| Path | What it owns |
|---|---|
| 🟡 `server/ai/mcp/registry.ts` | Which tools MCP exposes + capability filtering. |
| 🟡 `server/ai/mcp/editorBridge.ts` | Routes browser tools to the open workspace. |
| 🟡 `server/ai/mcp/tools/` | `contextTool`, `documentTools`, `styleTools`, `publishTool`, `studioImportTool`, `studio/` (project orientation, bulk edits, codemods, fidelity report, visual-audit family — `screenshot.ts`/`exportFrames.ts`/`referenceRender.ts`/`diffFrames.ts`). |
| 🟢 `server/ai/mcp/tools/studio/screenshot.ts` | `studio_screenshot` — the agent's eyes, and the one Studio tool the authoring loop needs. Reconciles board frames with disk (`syncBoardFramesFromDisk`), awaits the canvas re-read (`awaitStudioLiveReload`), then relays to the browser-side `studio_export_frames` capture. Three steps in one call because a partial ritual returns a stale image that reads as evidence. Pages are selected by NAME (`pageKey` applies `pageIdFromRelPath`'s kebab derivation to both sides). |
| 🟢 `server/ai/mcp/tools/studio/measureReference.ts` | `studio_measure_reference` — reads the DESIGN's own colours and type sizes (`studio_compare` scores the output; this reads the input). Rectangles in reference px; returns colours with the matching project token, a font-size RANGE, and a measured line-height. **Everything in CSS px**, scaled by frame-width/reference-width — a 2x comp would otherwise report a 21px heading as 42px. Engine: `server/handlers/studio/referenceMeasure.ts`. |
| 🟢 `server/ai/mcp/tools/studio/extractReferenceAsset.ts` | `studio_extract_reference_asset` — crops a rectangle out of a registered design reference and lands it as a real project PNG (`landAssetBytes`). The only reachable source for artwork that exists solely inside a pasted comp. Bytes never transit the model; an out-of-bounds crop is refused, never clamped. |
| 🟢 `server/ai/mcp/tools/studio/referenceResolve.ts` | `resolveDesignReference`/`cssPxPerReferencePx` — which registered reference a call means (explicit id → page-scoped → most recent) and the reference-px→CSS-px scale. Shared by `compare.ts` and `measureReference.ts` so the two can never disagree about which design they mean. |
| 🟢 `server/handlers/studio/referenceMeasure.ts` | The measurement engine behind `studio_measure_reference`: per-region background/foreground/palette, ink-row line detection, font-size range, line pitch. Colours bucket for RANKING but report the modal EXACT value (`#0c9ab0`, never the bucket centre `#1098b0`). |
| 🟢 `server/handlers/studio/colorMath.ts` | `parseHexColor`/`colorDifference` (CIE76 ΔE over CIELAB)/`contrastRatio`/`relativeLuminance`. Perceptual, not RGB distance — RGB calls two near-blacks adjacent and two different mid-greens close, which would name the wrong token confidently. |
| 🟢 `server/handlers/studio/projectTokenIndex.ts` | `buildProjectTokenIndex`/`nearestSizeToken` — the project's CSS custom properties indexed so a MEASURED value can be answered with the token that carries it. Sourced from the same CSS the canvas gets (`compileProjectStyles`'s `vendorCss` + `css`), deliberately NOT `.studio/framework.json` (a different scale; offering both would give two names from two systems). |
| 🟢 `server/handlers/studio/turnDesignReferences.ts` | `registerTurnDesignReferences` — arms every chat-attached image as a durable design reference before the system prompt is built, so `studio_compare` has a baseline on the turn the design arrives. Idempotent by content hash (runs every turn; conversations re-send attachments), never fatal. Lives here and not in `server/ai/handlers/`, which `ai-handlers-capability-gated.test.ts` requires every file of to call `requireCapability`. |
| 🟢 `server/ai/tools/studio/agentToolNames.ts` | `STUDIO_AGENT_TOOL_NAMES` — the explicit list of Studio tools the in-canvas agent is offered, as a dependency-free leaf both `index.ts` (resolves to real `AiTool`s, throws on an orphaned name) and `systemPrompt.ts` (renders the "Tools available" line) read. Written out by name so adding a registry tool does not silently widen the agent's surface. |
| 🟢 `server/ai/drivers/claudeCliToolSurface.ts` | `resolveNativeToolAllowlist` — the `--tools` allowlist. `Read,Write,Edit,Glob,Grep` with a project open (bounded by the subprocess `cwd`, not by the list); `Read` for an attachment-only turn; `''` otherwise. `Bash` and `Task` never, at any tier, in any mode — the doc comment explains why `Task` is a correctness fix, not a security one. |
| 🟢 `server/ai/drivers/claudeCliPermissionMode.ts` | `resolvePermissionMode`/`assertBypassOnlyFromExplicitRequest` — `acceptEdits` is the default with a project open (the agent's whole job is editing files in it), `default` otherwise; `bypassPermissions` only ever from an explicit per-turn user choice. |
| 🟡 `server/ai/mcp/resources.ts` | Static MCP resources (`studio://guidelines`) — read-only reference content, not tools. |
| 🟡 `server/ai/tools/site/` | The built-in agent's site tools, incl. `site_render_snapshot`. |

## Tests

| Path | What it owns |
|---|---|
| 🟡 `src/__tests__/architecture/` | ~90 gate tests. **If you change a structural rule, change its gate here.** |
| 🟢 `src/core/page-parser/__tests__/` | Evaluator, inlining, loop expansion, generic repo shapes. |
| 🟢 `src/__tests__/studio/` | Studio behaviour tests. |
| 🟡 `src/__tests__/setup.ts` | happy-dom setup, iframe global patching. |
| 🟡 `src/admin/pages/site/canvas/__tests__/iframeCanvasQuery.ts` | **Required** to query canvas DOM in tests. |
| 🟡 `tests/e2e/` | Playwright. Studio-relevant specs only. |

## Not ours (dormant CMS)

⚪ `server/handlers/cms/` · `server/repositories/` · `server/db/` ·
`server/publish/` · `server/plugins/` · `src/admin/pages/{dashboard,plugins,users}/` ·
`src/core/publisher/` · `src/core/plugin-sdk/` · `src/core/plugins/`

(`src/admin/pages/{content,data,media}/` do not exist on disk — Studio never
built a separate content/data workspace or media library page.)

Touch only when a Studio change genuinely requires it, and say so in the handoff.

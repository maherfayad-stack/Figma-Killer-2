.git
.worktrees
.DS_Store
node_modules
dist
.tmp
uploads
data
backups
# User data and private runtime state from a local Studio run: the users'
# projects, and .data/ (MCP secrets, Claude CLI logins). Never build context.
studio-workspace
.data
*.sqlite
*.sqlite-*
.env
.env.*
!.env.example
!.env.production.example
docs/superpowers

# Test sources are verified in CI but are not needed in production images.
**/__tests__
**/*.test.ts
**/*.test.tsx

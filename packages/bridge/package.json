{
  "name": "@ccs/bridge",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "description": "Script injected into file-app iframes: hit-test, rect reporting, hover-highlight postMessage protocol. P2 scope.",
  "main": "./src/index.ts",
  "types": "./src/index.ts",
  "exports": {
    ".": "./src/index.ts"
  },
  "scripts": {
    "typecheck": "tsc --noEmit -p tsconfig.json",
    "lint": "eslint .",
    "test": "vitest run"
  },
  "dependencies": {
    "zod": "catalog:"
  },
  "devDependencies": {
    "jsdom": "catalog:",
    "typescript": "catalog:",
    "vitest": "catalog:"
  }
}

{
  "name": "@ccs/protocol",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "description": "Frozen (P0) cross-boundary contract: NodeUid, CanvasOp, DaemonEvent, FrameMeta, TreeNode — zod schemas + inferred types.",
  "main": "./src/index.ts",
  "types": "./src/index.ts",
  "exports": {
    ".": "./src/index.ts"
  },
  "scripts": {
    "typecheck": "tsc --noEmit -p tsconfig.json",
    "lint": "eslint .",
    "test": "vitest run"
  },
  "dependencies": {
    "zod": "catalog:"
  },
  "devDependencies": {
    "typescript": "catalog:",
    "vitest": "catalog:"
  }
}

{
  "name": "@ccs/ui",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "description": "Studio's own design system (Radix/shadcn-based, modeled on Penpot per ADR-0007 — NOT themed by the Almosafer DS). P5 scope.",
  "main": "./src/index.ts",
  "types": "./src/index.ts",
  "exports": {
    ".": "./src/index.ts"
  },
  "scripts": {
    "typecheck": "tsc --noEmit -p tsconfig.json",
    "lint": "eslint .",
    "test": "vitest run"
  },
  "dependencies": {
    "react": "catalog:"
  },
  "devDependencies": {
    "@types/react": "catalog:",
    "typescript": "catalog:",
    "vitest": "catalog:"
  }
}

{
  "name": "@ccs/ast-engine",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "description": "Pure ts-morph codemod engine: applyOp(sourceText, op) -> {newText, uidRemap}. Golden-file test suite is the P3 critical path (playbook §4/P3).",
  "main": "./src/index.ts",
  "types": "./src/index.ts",
  "exports": {
    ".": "./src/index.ts"
  },
  "scripts": {
    "typecheck": "tsc --noEmit -p tsconfig.json",
    "lint": "eslint .",
    "test": "vitest run"
  },
  "dependencies": {
    "@ccs/protocol": "workspace:*",
    "prettier": "catalog:",
    "synckit": "catalog:",
    "ts-morph": "catalog:"
  },
  "devDependencies": {
    "@ccs/vite-plugin-source-uid": "workspace:*",
    "@types/node": "catalog:",
    "typescript": "catalog:",
    "vitest": "catalog:"
  }
}
